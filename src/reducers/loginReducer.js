import { START_LOGIN, FINISH_LOGIN } from "../actions/types";

const initialState = {
  loginState: ""
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
